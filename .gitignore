# Node
node_modules/
dist/
build/
*.log
npm-debug.log*
.pnpm-store/

# Env / secrets (NEVER commit)
.env
.env.*
!.env.example
*.key
*.pem
*.seed
wallet-*.json

# Midnight / Compact build artifacts
**/managed/
**/build/
*.wasm
.compact/
proof-server-data/
.midnight/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Test / coverage
coverage/
.nyc_output/

# Internal working docs — NOT for the public hackathon repo.
# Kept locally; transfer to the Ubuntu server manually.
docs/
candor-brand.zip

# Compiler output
contract/src/managed/

# Playwright / local inspection artifacts
.playwright-mcp/
svgterm-check.png

# Private-state store written by the devnet run — contains customer witness data
midnight-level-db/

# UI build output
dist-ui/

# Worktrees created by background agents
.claude/worktrees/

# Design/review skills installed for this project — tooling, not project content
.agents/
.claude/
skills-lock.json

# Written by `npm run devnet` after a deploy — points at whichever local
# devnet instance ran it. Meaningless (and privacy-irrelevant, but still
# machine-specific) to commit; the page falls back to its own demo book
# when this file is absent.
ui/chain.json
